# Example SOPS-encrypted secret (placeholder)
apiVersion: v1
kind: Secret
metadata:
  name: demo-secret
  namespace: dev
  annotations:
    # sops uses this annotation when encrypting
    sops.k8s.amazonaws.com/lastmodified: ""
type: Opaque
data:
  # example: username: base64
  username: ENC[AES256_GCM,data:...,iv:...,tag:...,type:str]
