Vagrant.configure("2") do |config|
  config.vm.box = "generic/ubuntu2204"

  config.vm.provider :libvirt do |v|
    v.cpus = 1
    v.memory = 1024
  end

  config.vm.define "alrahmouS" do |srv|
    srv.vm.hostname = "alrahmouS"
    srv.vm.network "private_network",
      ip: "192.168.56.110",
      libvirt__dhcp_enabled: false

    # Forward Traefik node ports so services are reachable from the host
    srv.vm.network "forwarded_port", guest: 31056, host: 31056, auto_correct: true
    srv.vm.network "forwarded_port", guest: 31848, host: 31848, auto_correct: true

    srv.vm.provision "file", source: "confs", destination: "/home/vagrant/confs"
    srv.vm.provision "shell", path: "scripts/server.sh"
  end
end
