# Example SealedSecret (Bitnami SealedSecrets controller required)
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: demo-sealed
  namespace: dev
spec:
  encryptedData:
    password: AgB...PLACEHOLDER...==
